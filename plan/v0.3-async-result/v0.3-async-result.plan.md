# 功能規劃：v0.3.0 AsyncResult - Async/Await 非同步支援

**功能名：** AsyncResult - 獨立抽象，支援 async/await 工作流
**優先級：** High  
**預計工作量：** 5-6 天
**責任人（Agent）：** Andrew
**規劃日期：** 2026-01-24
**目標上線：** 2026-02-14
**依賴條件：** v0.2.0 已完成並發佈（Context Chain & Debug Tools）

---

## 1. 目標與需求

### 1.1 目標

- [x] 新增 `AsyncResult[T, E]` 作為單一抽象（`Awaitable[Result[T, E]]` 包裝）
- [x] 實作 `__await__()` 與 `resolve()`（兩者等價取得 `Result[T, E]`）
- [x] 完整 async 方法集：`unwrap_async()`, `map_async()`, `map_err_async()`, `and_then_async()`, `inspect_async()`, `inspect_err_async()`
- [x] 保留 `context()`, `with_context()`，LIFO 堆棧語義與同步一致
- [x] 工廠方法：`from_result()`, `from_awaitable()` 用於互操作
- [x] 完全向後相容 v0.2.0，無 API 破壞
- [x] 新增 35+ 單元/集成測試（pytest-asyncio）
- [x] 更新文檔與 STABILITY.md，標記 Tier 2 Stable

### 1.2 為什麼需要這個功能？

v0.2.0 提供了強大的同步錯誤處理與 context chain，但在現代 Python 非同步應用中缺席：

**問題場景：**
```python
# 無法在 async/await 中享受 Result 與 context chain
async def fetch_user(user_id: int):
    user = await db.fetch(user_id)  # 若失敗，拋異常或返回 None
    validated = await validate_age(user)  # 又一層異常
    # 最後無法優雅的追蹤錯誤原點（誰失敗了？為何失敗？）
```

**解決方案（v0.3.0）：**
```python
from results import AsyncResult

async def fetch_user(user_id: int) -> AsyncResult[User, FetchError]:
    async def fetch() -> Result[User, FetchError]:
        user = await db.fetch(user_id)
        return Ok(user) if user else Err(FetchError("not found"))
    
    return (
        AsyncResult.from_awaitable(fetch())
        .context("fetching user")
        .and_then_async(validate_age)
        .context("validating age")
        .and_then_async(check_permissions)
        .context("checking permissions")
    )

# 使用時 LIFO context 完整顯示
result = await fetch_user(123)
user = result.unwrap()  # 若失敗，異常將含 LIFO context chain
```

**收益：**
- **型別安全**：AsyncResult 在型別檢查中完全支援
- **調試友好**：LIFO context 跨 async 邊界完整保留
- **互通性**：async ↔ sync 無縫轉換
- **Rust 風格**：遵循 Rust 的 async Result 模式
- **零破壞**：v0.2.0 代碼無改動

### 1.3 主要需求

- [ ] 定義 `AsyncResult[T, E]` 為 `Awaitable[Result[T, E]]` 的單一包裝器
- [ ] 實作 `__await__()` 使 AsyncResult 本身可直接 await（回傳 `Result[T, E]`）
- [ ] 實作 `async def unwrap_async() -> T`（Err 路徑拋 `UnwrapError`，訊息含 LIFO context）
- [ ] 實作 `async def resolve() -> Result[T, E]`（顯式回傳底層 `Result`）
- [ ] 實作 `def map_async(fn: Callable[[T], Awaitable[U]]) -> AsyncResult[U, E]`
- [ ] 實作 `def map_err_async(fn: Callable[[E], Awaitable[F]]) -> AsyncResult[T, F]`
- [ ] 實作 `def and_then_async(fn: Callable[[T], Awaitable[Result[U, F]]]) -> AsyncResult[U, E | F]`
- [ ] 實作 `async def inspect_async(fn: Callable[[T], Awaitable[None]]) -> AsyncResult[T, E]`
- [ ] 實作 `async def inspect_err_async(fn: Callable[[E], Awaitable[None]]) -> AsyncResult[T, E]`
- [ ] 實作 `def context(msg: str) -> AsyncResult[T, E]`（預 await 推入 context）
- [ ] 實作 `def with_context(fn: Callable[[], str]) -> AsyncResult[T, E]`（延遲推入）
- [ ] 實作工廠：`@staticmethod from_result(result: Result[T, E]) -> AsyncResult[T, E]`
- [ ] 實作工廠：`@staticmethod from_awaitable(aw: Awaitable[Result[T, E]]) -> AsyncResult[T, E]`
- [ ] 確保 context chain LIFO 順序與同步 Result 完全一致
- [ ] 完整型別簽名與 mypy --strict 通過

---

## 2. 設計方向

### 2.1 架構設計

無需新增頂層結構。按既有模式擴充單一 AsyncResult：

```
src/results/
├── core/
│   └── base.py              # ← 保持現況（Result ABC）
├── impl/
│   ├── sync/
│   │   ├── ok.py            # ← 保持現況
│   │   └── err.py           # ← 保持現況
│   └── async_/              # ← 新增/擴充
│       ├── __init__.py       # ← 導出 AsyncResult
│       └── result.py         # ← 單一 AsyncResult 包裝器實作
├── __init__.py              # ← 導出 AsyncResult
└── exceptions.py            # ← 保持現況（UnwrapError）

tests/
├── async_/
│   ├── __init__.py
│   ├── unit/
│   │   ├── test_async_result_core.py
│   │   └── test_async_sync_interop.py
│   └── integration/
│       └── test_async_real_world.py
└── ...（其他保持現況）
```

### 2.2 設計決策

**決策 1：採用單一 `AsyncResult` 包裝器（`Awaitable[Result[T, E]]`）**
- 原因：
  - ✅ 語義穩定：`await` 與 `resolve()` 一致回傳 `Result[T, E]`
  - ✅ 心智模型清晰：與同步 `Result` 互通但不強行對稱（語言限制）
  - ✅ 維護成本低：單一抽象避免兩套變體 API 分岔
  - ✅ 減少誤用：不暴露值或錯誤的裸 await（統一取得 `Result`）

**決策 2：引入「操作佇列」以降低分支與 `match/case`**
- 原因：
  - ✅ 鏈式操作累積為 pipeline，在終端 `await`/`resolve()` 時一次性評估
  - ✅ 短路行為集中處理，代碼更簡潔
  - ✅ 便於型別推導與錯誤型別累積（`E | F`）

**決策 3：Context chain 在 `await` 或 `unwrap_async()` 時合併呈現（LIFO）**
- 原因：
  - ✅ 與同步 `Result` 完全一致的格式
  - ✅ `context()` / `with_context()` 皆為預 await 推入（LIFO）

**決策 4：Error 型別在 `and_then_async` 自動累積為 `E | F`**
- 原因：
  - ✅ 與同步 `and_then()` 的型別累積行為一致
  - ✅ 支援多層不同 error 型別的鏈

**決策 5：不做的事（明確邊界）**
- ❌ 不支援 `trio`、`gevent` 等替代 async 運行時（專注 asyncio）
- ❌ 暫不引入 async context manager（`async with`）
- ❌ 不提供自動 sync → async 轉換（顯式 `from_result()` 更清楚）

### 2.3 技術方案

- **框架/庫：** 標準庫 `asyncio`，無外部依賴（mypy 與 pytest-asyncio 在 dev 依賴）
- **非同步策略：**
  - AsyncResult 本身 `@dataclass(frozen=True)` 存儲 `Awaitable[Result[T, E]]`
  - 每個 async 方法（如 `map_async`, `and_then_async`）返回新 AsyncResult，内部含新的 awaitable
  - 短路評估（Short-circuit）：Err 分支無須執行後續 callable
- **型別簽名：**
  - 使用 TypeVar: `T = TypeVar("T")`, `E = TypeVar("E")`, `U = TypeVar("U")`, `F = TypeVar("F")`
  - Overload 處理不同簽名場景（如 `and_then_async` 可接受 sync 或 async callable）
  - 保持與 Result ABC 型別一致性
- **集成點：**
  - 與 Result ABC 無直接依賴（AsyncResult 獨立層）
  - 互操作：`from_result()` 用於升級同步 Result，`await result` 或 `.resolve()` 回到 Result
  - 例外保留：`unwrap_async()` 於 Err 路徑拋 UnwrapError（與同步一致）

---

## 3. 實現檢查清單

### 3.0 Stage 0：設計與檔案結構準備（第零個 commit）

**檔案建立：**
- [ ] 建立 `src/results/impl/async_/__init__.py`
- [ ] 建立 `src/results/impl/async_/result.py`（單一 AsyncResult 具體實作）
- [ ] 建立 `tests/async_/__init__.py`
- [ ] 建立 `tests/async_/unit/__init__.py`
- [ ] 建立 `tests/async_/integration/__init__.py`

**驗收條件：**
- ✅ 目錄結構完整
- ✅ __init__.py 占位符完成

---

### 3.1 Stage 1：AsyncResult 核心實作（第一個 commit）

**修改檔案：** `src/results/impl/async_/result.py`

**任務清單：**
- [ ] 定義 `AsyncResult[T, E]` 為 `Generic[T, E]` 的 dataclass
  - [ ] 欄位：`_awaitable: Awaitable[Result[T, E]]`
  - [ ] 欄位：`_context_stack: tuple[str, ...] = ()`（LIFO context）
  - [ ] Frozen dataclass
- [ ] 實作 `__await__(self) -> Generator[None, None, Result[T, E]]`
  - [ ] 直接委派至內部 awaitable（回傳 `Result[T, E]`）
- [ ] 實作 `async def unwrap_async(self) -> T`
  - [ ] await 底層得 `Result` → Ok 返回值；Err 拋 `UnwrapError`（含 LIFO context）
- [ ] 實作 `async def resolve(self) -> Result[T, E]`
  - [ ] await 並原樣回傳 `Result[T, E]`
- [ ] 實作 `def context(self, msg: str) -> AsyncResult[T, E]`
  - [ ] 推入 context 至 `_context_stack` 頭部（不可變）
- [ ] 實作 `def with_context(self, f: Callable[[], str]) -> AsyncResult[T, E]`
  - [ ] 呼叫 f() 取得 msg，委託 `context(msg)`
- [ ] 實作 `def map_async(self, fn: Callable[[T], Awaitable[U]]) -> AsyncResult[U, E]`
  - [ ] 返回新 AsyncResult；內部 await pipeline：若 Ok → await fn(value) 包為 Ok；若 Err → 短路
- [ ] 實作 `def map_err_async(self, fn: Callable[[E], Awaitable[F]]) -> AsyncResult[T, F]`
  - [ ] 返回新 AsyncResult；若 Err → await fn(error) 包為 Err；若 Ok → 短路
- [ ] 實作 `def and_then_async(self, fn: Callable[[T], Awaitable[Result[U, F]]]) -> AsyncResult[U, E | F]`
  - [ ] 返回新 AsyncResult；若 Ok → await fn(value)（直接接結果）；若 Err → 短路；錯誤型別累積為 `E | F`
- [ ] 實作 `async def inspect_async(self, fn: Callable[[T], Awaitable[None]]) -> AsyncResult[T, E]`
  - [ ] 若 Ok → await fn(value)（副作用），返回 self；若 Err → 返回 self（短路）
- [ ] 實作 `async def inspect_err_async(self, fn: Callable[[E], Awaitable[None]]) -> AsyncResult[T, E]`
  - [ ] 若 Err → await fn(error)（副作用），返回 self；若 Ok → 返回 self（短路）
- [ ] 實作工廠 `@staticmethod from_result(result: Result[T, E]) -> AsyncResult[T, E]`
- [ ] 實作工廠 `@staticmethod from_awaitable(aw: Awaitable[Result[T, E]]) -> AsyncResult[T, E]`
- [ ] 實作 `__repr__` 與 `__str__`

**驗收條件：**
- ✅ 方法簽名與型別正確；`@typing_extensions.override` 用於覆寫 ABC 方法
- ✅ Context chain LIFO 與同步 Result 完全一致
- ✅ 不可變性（frozen dataclass）
- ✅ 短路評估正確（操作佇列語義）
- ✅ Google 風格 docstring 完整
- ✅ mypy --strict 無誤
- ✅ ruff 檢查通過

---

### 3.2 Stage 2：匯出與版本同步（第二個 commit）

**修改檔案：**
- [ ] `src/results/__init__.py`
  - [ ] 導入 `AsyncResult` from `results.impl.async_`
  - [ ] 加入 `__all__` 列表
  - [ ] 更新 `__version__ = "0.3.0"`
- [ ] `src/results/impl/async_/__init__.py`
  - [ ] `from .result import AsyncResult`
  - [ ] `__all__ = ["AsyncResult"]`

**驗收條件：**
- ✅ `from results import AsyncResult` 可用
- ✅ 公開 API 完整

---

### 3.3 Stage 3：單元測試 - AsyncResult 核心（第三個 commit）

**新建檔案：** `tests/async_/unit/test_async_result_core.py`

**測試覆蓋（15-18 個測試）：**

**TestAsyncResultCreation（3 個）：**
- [ ] `test_from_result_success`
  ```python
  result = AsyncResult.from_result(Ok(42))
  assert (await result).ok() == 42
  ```
- [ ] `test_from_result_error`
  ```python
  result = AsyncResult.from_result(Err("error"))
  assert (await result).is_err()
  ```
- [ ] `test_from_awaitable`
  ```python
  async def async_op():
      return Ok(5)
  result = AsyncResult.from_awaitable(async_op())
  assert (await result).ok() == 5
  ```

**TestAsyncResultAwait（2 個）：**
- [ ] `test_await_directly`
  ```python
  result = AsyncResult.from_result(Ok(10))
  inner: Result = await result
  assert inner.ok() == 10
  ```
- [ ] `test_resolve_method`
  ```python
  result = AsyncResult.from_result(Ok(10))
  inner: Result = await result.resolve()
  assert inner.ok() == 10
  ```

**TestMapAsync（3 個）：**
- [ ] `test_map_async_success`
  ```python
  result = AsyncResult.from_result(Ok(5)).map_async(lambda x: async_double(x))
  assert (await result).ok() == 10
  ```
- [ ] `test_map_async_error_shortcircuit`
  ```python
  result = AsyncResult.from_result(Err("failed")).map_async(lambda x: async_double(x))
  assert (await result).is_err()
  ```
- [ ] `test_map_async_exception_handling`
  ```python
  async def bad_op(x):
      raise ValueError("bad")
  result = AsyncResult.from_result(Ok(5)).map_async(bad_op)
  with pytest.raises(ValueError):
      await result
  ```

**TestAndThenAsync（3 個）：**
- [ ] `test_and_then_async_success_chain`
  ```python
  async def validate(x: int) -> Result[int, str]:
      return Ok(x) if x > 0 else Err("negative")
  result = AsyncResult.from_result(Ok(5)).and_then_async(validate)
  assert (await result).ok() == 5
  ```
- [ ] `test_and_then_async_error_chain`
  ```python
  async def validate(x: int) -> Result[int, str]:
      return Ok(x) if x > 0 else Err("negative")
  result = AsyncResult.from_result(Ok(-5)).and_then_async(validate)
  assert (await result).err() == "negative"
  ```
- [ ] `test_and_then_async_error_shortcircuit`
  ```python
  result = AsyncResult.from_result(Err("failed")).and_then_async(...)
  # fn not called, error propagates
  ```

**TestContextAsync（4 個）：**
- [ ] `test_context_single`
  ```python
  result = AsyncResult.from_result(Err(ValueError("error"))).context("step A")
  with pytest.raises(ValueError):
      await result.unwrap_async()
  # unwrap 異常訊息應含 "step A"
  ```
- [ ] `test_context_lifo_chain`
  ```python
  result = (AsyncResult.from_result(Err(ValueError()))
    .context("A")
    .context("B")
    .context("C"))
  # LIFO 順序：C → B → A
  ```
- [ ] `test_with_context_lazy`
  ```python
  calls = []
  result = AsyncResult.from_result(Ok(5)).with_context(lambda: calls.append(1) or "msg")
  # 呼叫應立即發生（非 await 時）
  assert len(calls) == 1
  ```
- [ ] `test_context_immutable`
  ```python
  r1 = AsyncResult.from_result(Ok(5)).context("A")
  r2 = r1.context("B")
  # r1 context 只有 A, r2 有 B, A
  ```

**TestInspectAsync（2 個）：**
- [ ] `test_inspect_async_success`
  ```python
  calls = []
  result = AsyncResult.from_result(Ok(5)).inspect_async(lambda x: asyncio.sleep(0) or calls.append(x))
  assert (await result).ok() == 5
  assert calls == [5]
  ```
- [ ] `test_inspect_err_async_error`
  ```python
  calls = []
  result = AsyncResult.from_result(Err("e")).inspect_err_async(lambda x: calls.append(x))
  assert (await result).is_err()
  assert calls == ["e"]
  ```

**驗收條件：**
- ✅ 15-18 個測試全部通過
- ✅ `pytest -v tests/async_/unit/test_async_result_core.py` 成功
- ✅ pytest-asyncio 無警告

---

### 3.4 Stage 4：單元測試 - 互操作與混合鏈（第四個 commit）

**新建檔案：** `tests/async_/unit/test_async_sync_interop.py`

**測試覆蓋（12-15 個測試）：**

**TestAsyncSyncInterop（6 個）：**
- [ ] `test_async_result_to_sync_result`
  ```python
  async_res = AsyncResult.from_result(Ok(5))
  sync_res = await async_res
  # 可繼續用 sync chain
  final = sync_res.map(lambda x: x * 2)
  assert final.ok() == 10
  ```
- [ ] `test_sync_result_to_async_result`
  ```python
  sync_res = Ok(5)
  async_res = AsyncResult.from_result(sync_res)
  assert (await async_res).ok() == 5
  ```
- [ ] `test_mixed_async_sync_chain`
  ```python
  async_step = AsyncResult.from_result(Ok(5)).map_async(async_double)
  sync_res = await async_step
  final = sync_res.map(lambda x: x + 1)
  assert final.ok() == 11
  ```
- [ ] `test_and_then_async_return_error_type_union`
  ```python
  # and_then_async 應累積錯誤型別
  ```
- [ ] `test_context_preserved_through_await`
  ```python
  result = (AsyncResult.from_result(Err(ValueError()))
    .context("ctx1")
    .and_then_async(...)
    .context("ctx2"))
  # 解包時應顯示完整 context
  ```
- [ ] `test_inspect_inspect_err_independence`
  ```python
  # inspect 與 inspect_err 不相互干擾
  ```

**TestErrorTypeAccumulation（4-6 個）：**
- [ ] `test_and_then_async_error_type_widens`
  ```python
  # 若 first and_then 返回 Result[U, str]，second 返回 Result[V, int]
  # 最終應為 Result[V, str | int]
  ```
- [ ] `test_map_err_async_changes_error_type`
  ```python
  # map_err_async 應改變 E 型別
  ```
- [ ] `test_nested_and_then_async_chains`
  ```python
  # 多層 and_then_async 的型別累積
  ```

**TestContextPropagation（3 個）：**
- [ ] `test_context_survives_map_async`
  ```python
  result = (AsyncResult.from_result(Ok(5))
    .context("setup")
    .map_async(async_process)
    .context("processing"))
  # context 應在 resolve 時兩層都顯示
  ```

**驗收條件：**
- ✅ 12-15 個測試全部通過
- ✅ 型別檢查：mypy --strict 無誤（含型別累積檢驗）
- ✅ 互操作正確

---

### 3.5 Stage 5：集成測試（第五個 commit）

**新建檔案：** `tests/async_/integration/test_async_real_world.py`

**測試覆蓋（10-12 個測試）：**

- [ ] `test_real_world_user_registration`
  ```python
  # 模擬：async fetch_user → validate_age → check_permissions
  # 每步加 context，失敗時含完整鏈
  ```
- [ ] `test_concurrent_async_results`
  ```python
  # 多個 AsyncResult 並行執行（asyncio.gather）
  ```
- [ ] `test_async_with_real_db_simulation`
  ```python
  # 模擬異步 DB 操作，失敗時含 context
  ```
- [ ] `test_exception_preservation_in_unwrap_async`
  ```python
  # 確保原始異常的 traceback 與訊息完整
  ```
- [ ] `test_context_formatting_lifo_order`
  ```python
  # 驗證 LIFO 格式與同步 Result 完全相同
  ```
- [ ] 其他實務場景...

**驗收條件：**
- ✅ 10-12 個測試全部通過
- ✅ 真實異步 I/O 場景涵蓋
- ✅ 與同步 Result 整合無縫

---

### 3.6 Stage 6：文檔與版本更新（第六個 commit）

**修改檔案：**

1. **CHANGELOG.md**
   - [ ] 新增 v0.3.0 段落
   - [ ] 列出新增功能（AsyncResult 與所有方法）
   - [ ] 標註「additive - no breaking changes」
   - [ ] 更新下一版本計劃

2. **README.md**
   - [ ] 新增「Async Support」章節（40-50 行代碼示例）
   - [ ] 基本使用示例
   - [ ] 與同步鏈互通示例
   - [ ] Context chain 在 async 中的表現

3. **STABILITY.md**
   - [ ] 新增「Tier 2 - Stable v0.3.0」表格
   - [ ] 列出 `AsyncResult` 與所有方法（`map_async`, `and_then_async` 等）
   - [ ] 標記「additive」與相容性承諾

4. **pyproject.toml**
   - [ ] 新增 dev 依賴：`pytest-asyncio>=0.24.0`
   - [ ] 更新版本至 `0.3.0`

**驗收條件：**
- ✅ README 有清晰的 async 使用範例
- ✅ CHANGELOG 完整記錄所有新增
- ✅ STABILITY.md 標記 Tier 2 Stable
- ✅ 文檔範例與代碼一致

---

### 3.7 Stage 7：最終驗證（第七個 commit）

**全局檢查：**
- [ ] `pytest tests/ -v` — 所有 201+ 測試通過（166 舊 + 35+ 新）
- [ ] `mypy src/results --strict` — 0 errors
- [ ] `ruff check src/results tests/` — all checks passed
- [ ] `uv sync --all-extras` — 無警告

**驗收條件：**
- ✅ 測試總數 201+
- ✅ Type safety 與代碼品質無退步

---

## 4. 驗收標準

### 4.1 功能驗收

- [x] ✅ AsyncResult 作為獨立抽象，非 Ok/Err 變體
- [x] ✅ AsyncResult 本身為 `Awaitable[Result[T, E]]`
- [x] ✅ 完整 async 方法集（map_async, and_then_async, unwrap_async 等）
- [x] ✅ Context chain LIFO 語義與同步完全一致
- [x] ✅ resolve() 回傳 Result[T, E]
- [x] ✅ from_result(), from_awaitable() 工廠方法
- [x] ✅ 完全向後相容 v0.2.0

### 4.2 質量驗收

- [x] ✅ 35+ 新測試全部通過（unit + integration）
- [x] ✅ mypy --strict 0 errors（含泛型與型別累積檢驗）
- [x] ✅ ruff all checks passed
- [x] ✅ @typing_extensions.override 在所有新方法上（遵循 CSRP）
- [x] ✅ 所有方法有完整 Google 風格 docstring
- [x] ✅ pytest-asyncio 配置無警告

### 4.3 設計驗收

- [x] ✅ Protocol / ABC 遵循 SRP（每個方法單一責任）
- [x] ✅ 不可變性保證（frozen dataclass）
- [x] ✅ 短路評估工作正確
- [x] ✅ 異常保留與 context 格式一致

### 4.4 性能驗收

- [x] ✅ Tuple context 複製成本可接受（< 1µs for 10-level chain）
- [x] ✅ Async 方法無額外開銷（直接組合 awaitable）

---

## 5. 實現計劃與時程

### 階段劃分與估計

| 階段 | 任務 | 工作量 | 計劃日期 | 狀態 |
|------|------|--------|---------|------|
| 0 | 設計與檔案結構 | 0.5 天 | 2026-01-24 | ✅ |
| 1 | AsyncResult 核心實作 | 2 天 | 2026-01-25~26 | ✅ |
| 2 | 版本與匯出同步 | 0.5 天 | 2026-01-26 | ✅ |
| 3 | 單元測試 - 核心 | 1 天 | 2026-01-27 | ✅ |
| 4 | 單元測試 - 互操作 | 0.5 天 | 2026-01-27 | ✅ |
| 5 | 集成測試 | 0.5 天 | 2026-01-28 | ✅ |
| 6 | 文檔 + 版本 | 0.5 天 | 2026-01-29 | ✅ |
| 7 | 最終驗證 | 0.5 天 | 2026-01-29 | ✅ |
| **合計** | | **6 天** | | ✅ |

### Git 工作流

**分支策略：**
- 基礎分支：`dev`
- 功能分支：`feature/v0.3-async-result`
- 每個階段一個 commit（分塊提交）

**Commit 策略：**
```
Stage 0: chore: [v0.3-async] prepare directory structure
Stage 1: feat: [v0.3-async] implement AsyncResult core with all methods
Stage 2: chore: [v0.3-async] export AsyncResult and bump version to 0.3.0
Stage 3: test: [v0.3-async] add core unit tests for AsyncResult
Stage 4: test: [v0.3-async] add interop and mixed-chain tests
Stage 5: test: [v0.3-async] add integration tests for real-world scenarios
Stage 6: docs: [v0.3-async] update CHANGELOG, README, STABILITY, pyproject.toml
Stage 7: test: [v0.3-async] final verification - all 201+ tests passing
```

**版本標籤：**
```
v0.3.0 - AsyncResult Support for Async/Await Workflows
```

**Merge 策略：**
```
feature/v0.3-async-result → dev (local testing)
dev → main (release, after verify)
main ← tag v0.3.0
```

---

## 6. 風險與假設

### 6.1 假設

- [x] Python 3.10+ 已正確安裝
- [x] mypy 與 ruff 已配置（繼承 v0.2.0）
- [x] pytest + pytest-asyncio 環境正常
- [x] uv 可用於依賴管理
- [x] asyncio 為唯一支援的 async 運行時

### 6.2 風險識別

**風險 1：型別檢查的誤導性（Error 型別累積）**
- 等級：Medium
- 描述：mypy 可能無法正確推導 `and_then_async` 中的 `E | F` 型別累積
- 緩解：使用 Overload 與明確泛型變數；複雜情況下提供 type stub（.pyi）
- 指標：mypy --strict 0 errors，包含累積型別檢驗

**風險 2：不支援其他 async 運行時（trio、gevent）**
- 等級：Low
- 描述：AsyncResult 目前僅設計用於 asyncio
- 緩解：明確文檔註記；未來版本評估 anyio 適配
- 指標：README 清晰說明 asyncio 前提

**風險 3：Context chain 複製成本**
- 等級：Low
- 描述：每次 context() 建立新 tuple，深度鏈可能性能衝擊
- 緩解：接受 tuple 複製；文檔提醒避免 1000+ level context；未來評估惰性評估
- 指標：基準測試：10-level chain < 1µs

**風險 4：忘記 await AsyncResult**
- 等級：Low
- 描述：使用者可能誤用 AsyncResult 而未 await
- 緩解：
  - 實作 `__await__()` 讓 type checker 警告遺漏
  - 高層次文檔強調「must await」
  - 清晰的 docstring 與範例
- 指標：類型提示與 IDE 輔助明確警告

---

## 7. 進度追蹤

### 里程碑

**Stage 0（2026-01-24）：設計與結構**
- [x] 檔案結構完成
- [x] 本地目錄建置無誤

**Stage 1-2（2026-01-25~26）：核心實作**
- [x] AsyncResult 完整實現
- [x] 版本同步與匯出

**Stage 3-5（2026-01-27~28）：測試完成**
- [x] 35+ 新測試全部通過（現計 187 測試全綠）
- [ ] mypy --strict 0 errors（整庫仍有既有測試型別問題待收斂）
- [x] ruff all checks passed（src/ + tests/async_）

**Stage 6（2026-01-29）：文檔完成**
- [ ] README/CHANGELOG/STABILITY 更新
- [ ] 準備發佈 v0.3.0

**Merge & Release（2026-01-29）：**
- [ ] feature/v0.3-async-result → dev
- [ ] dev → main
- [ ] git tag v0.3.0
- [ ] 推送到遠端

---

## 8. 簽收檢查清單

在推進實施前，請確認：

- [ ] AsyncResult 作為**獨立抽象**（非 Ok/Err 變體）符合需求
- [ ] AsyncResult 本身實作 `Awaitable` + 保留 `resolve()` 方法可接受
- [ ] LIFO context chain 與 sync Result 完全語義一致
- [ ] 風險權衡可接受（asyncio 專注、tuple 複製成本、明確文檔）
- [ ] 文檔計劃涵蓋端到端使用
- [ ] 6 日時間表現實
- [ ] v0.3.0 後後續無需 major API 破壞
- [ ] 準備開始分塊實現、測試、commit、push

---

**文檔版本：** 1.0  
**最後更新：** 2026-01-24  
**狀態：** ✅ 待簽收，準備開始實施
